// Prettier, configured to agree with JavaScript Standard Style.
// Third-party code and markdown are excluded via .prettierignore.
module.exports = {
  ...require('prettier-config-standard'),
  arrowParens: 'avoid'
}
